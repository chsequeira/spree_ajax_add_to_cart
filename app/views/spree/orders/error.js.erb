<% 
  messages = []
  flash.each do |msg_type, text|
    messages << text
  end
  messages = messages.join('. ')
%>
$('#bottom_cart').html("<%= messages %>");
$('#bottom_flash_area').addClass('ajax-error-message');
$('#bottom_flash_area').show(100).delay(5000).hide(200);
